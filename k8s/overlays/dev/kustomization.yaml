apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: multiprompt-sandbox-dev

bases:
  - ../../base

namePrefix: dev-

patchesStrategicMerge:
  - replica-patch.yaml

configMapGenerator:
  - name: app-config
    behavior: merge
    literals:
      - environment=development
