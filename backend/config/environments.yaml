# Environment-specific configuration
# CORS allowed origins for different environments

development:
  allowed_origins:
    - http://localhost:4200
    - http://localhost:3000
    - http://localhost:8080
    - http://127.0.0.1:4200
    - http://127.0.0.1:3000

production:
  allowed_origins:
    # Cloud Run frontend URLs - both formats (short and full)
    - https://multiprompt-frontend-h7qqra6pma-uc.a.run.app
    - https://multiprompt-frontend-595703335416.us-central1.run.app
    # Support trailing slash variants
    - https://multiprompt-frontend-h7qqra6pma-uc.a.run.app/
    - https://multiprompt-frontend-595703335416.us-central1.run.app/
    # Legacy/fallback local URLs for admin access
    - http://localhost:4200
    - http://localhost:3000

staging:
  allowed_origins:
    - http://localhost:4200
    - http://localhost:3000
    # Add staging URLs here when available
